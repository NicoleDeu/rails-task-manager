<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<%if @task.completed %>
  <p><%= "The task is completed." %></p>
<% else %>
  <p><%= "This task is not completed yet." %></p>
<% end %>
<%= link_to "Back to tasks", tasks_path%>
